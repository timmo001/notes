---
name: Renovate automerge

# yamllint disable-line rule:truthy
on:
  pull_request_target: {}

jobs:
  automerge:
    uses: timmo001/workflows/.github/workflows/renovate-automerge.yml@e0ff73a140a248864e7fd3c459ed097841742bb6 # master
    permissions:
      pull-requests: write
      contents: write
