---
name: Renovate automerge

# yamllint disable-line rule:truthy
on:
  pull_request_target: {}

jobs:
  automerge:
    uses: timmo001/workflows/.github/workflows/renovate-automerge.yml@e4de786a4035b1fd50cd19a750a34d35e8403fbe # master
    permissions:
      pull-requests: write
      contents: write
