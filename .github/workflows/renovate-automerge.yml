---
name: Renovate automerge

# yamllint disable-line rule:truthy
on:
  pull_request_target: {}

jobs:
  automerge:
    uses: timmo001/workflows/.github/workflows/renovate-automerge.yml@a884a1a831cd482dfeff570bda660aee5baa328c # master
    permissions:
      pull-requests: write
      contents: write
