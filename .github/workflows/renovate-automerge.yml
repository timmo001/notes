---
name: Renovate automerge

# yamllint disable-line rule:truthy
on:
  pull_request_target: {}

jobs:
  automerge:
    uses: timmo001/workflows/.github/workflows/renovate-automerge.yml@d5ccbb565417449d2ece784ede889dc1fb9aee27 # master
    permissions:
      pull-requests: write
      contents: write
