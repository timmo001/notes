---
name: Renovate automerge

# yamllint disable-line rule:truthy
on:
  pull_request_target: {}

jobs:
  automerge:
    uses: timmo001/workflows/.github/workflows/renovate-automerge.yml@b49dfb01f5c8f63133cf80890b187406bdafc8bc # master
    permissions:
      pull-requests: write
      contents: write
