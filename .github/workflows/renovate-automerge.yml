---
name: Renovate automerge

# yamllint disable-line rule:truthy
on:
  pull_request_target: {}

jobs:
  automerge:
    uses: timmo001/workflows/.github/workflows/renovate-automerge.yml@a10505530cacb7a25adc0b948ee8ff0cc209cd0a # master
    permissions:
      pull-requests: write
      contents: write
