{
  "schemaVersion": 1,
  "id": "timmo.notes",
  "name": "Notes",
  "version": "1.0.0",
  "author": "Timmo",
  "license": "Apache-2.0",
  "description": "Browse, edit, create, and capture repository notes",
  "kinds": ["service", "bar-widget"],
  "keepLoaded": true,
  "entryPoints": {
    "service": "Service.qml",
    "barWidget": "BarWidget.qml"
  },
  "barWidget": {
    "displayName": "Notes",
    "description": "Browse and manage repository notes",
    "category": "Utility",
    "allowMultiple": false,
    "defaultSection": "right",
    "defaults": {
      "primaryOnly": true,
      "primaryOutput": "",
      "workspaceContextCommand": "",
      "workspaceContextFile": "",
      "workspaceContextRefreshCommand": ""
    },
    "schema": [
      {
        "key": "workspaceContextFile",
        "type": "string",
        "label": "Workspace context file",
        "defaultValue": "",
        "description": "Optional watched JSON file. Relative paths use XDG_RUNTIME_DIR. Takes precedence over command polling."
      },
      {
        "key": "workspaceContextRefreshCommand",
        "type": "string",
        "label": "Workspace context refresh command",
        "defaultValue": "",
        "description": "Optional command requesting a fresh context file when opening or refreshing the panel."
      },
      {
        "key": "workspaceContextCommand",
        "type": "string",
        "label": "Workspace context command",
        "defaultValue": "",
        "description": "Optional command returning JSON with attached and cwd. Empty disables workspace context."
      },
      {
        "key": "primaryOnly",
        "type": "boolean",
        "label": "Primary output only",
        "defaultValue": true
      },
      {
        "key": "primaryOutput",
        "type": "string",
        "label": "Primary output",
        "defaultValue": "",
        "description": "Optional output name used when primary output only is enabled."
      }
    ]
  }
}
